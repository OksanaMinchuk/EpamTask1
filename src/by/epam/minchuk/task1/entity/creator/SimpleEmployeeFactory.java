package by.epam.minchuk.task1.entity.creator;

import by.epam.minchuk.task1.entity.*;

/*public class SimpleEmployeeFactory {


    protected Employee createEmployee(TypeEmployee typeEmployee) {

        Employee employee = null;

        String name = null;
        String surname = null;
        int salaryPerHour = 0;
        Engineer.EngineerLevelType level = null;

        if (typeEmployee.equals(TypeEmployee.DEVELOPER)) {
            Developer.DeveloperType developerType = null;
            String skill = null;
            employee = new Developer(name, surname, salaryPerHour, level, developerType, skill);

        } else if (typeEmployee.equals(TypeEmployee.TESTER)) {
            Tester.TesterType testerType = null;
            employee = new Tester(name, surname, salaryPerHour, level, testerType);

        } else if (typeEmployee.equals(TypeEmployee.PROJECT_MANAGER)) {
            String project = null;
            employee = new ProjectManager(name, surname, salaryPerHour, project);
        }
        return employee;
    }
}*/
